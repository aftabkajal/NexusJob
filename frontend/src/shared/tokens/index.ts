// Design-token barrel. The CSS custom properties (`tokens.css`, `base.css`) are
// side-effect stylesheets imported directly in `app/App.tsx`; this barrel
// re-exports only the typed JS mirror.
export * from './tokens'
