// FSD `entities` layer: domain nouns with their query keys and API segments.
export {
  authClient,
  toApiError,
  type ApiError,
  sessionQueryKey,
  useSession,
  type SessionViewer,
} from './session'
