# Deferred Work

Append-only. Each entry is work identified but intentionally not done in the spec that surfaced it.

- source_spec: `spec-1-1-walking-skeleton.md`
  summary: `GET /health` happy path (200 + `{status:healthy,database:ok}`) has no automated test.
  evidence: The only test project is `NexusJob.ArchitectureTests`; none exercises `/health`. A real assertion on the DB-up path needs a live Postgres (Testcontainers), which spec 1.1's Boundaries deliberately scope to later stories. The DB-down path was patched with a `WebApplicationFactory` unit test in review pass 1; the DB-up path remains uncovered until an integration-test harness exists.

- source_spec: `spec-1-1-walking-skeleton.md`
  summary: No Vite dev-server `/api` proxy and no documented non-Docker dev loop.
  evidence: `frontend/vite.config.ts` defines no `server.proxy`, so `npm run dev` serves a SPA that cannot reach the Host's `/api` or `/health`. Story 1.1 has no API for the SPA to call and its Verification uses `docker compose`; the first story that makes real API calls from the SPA (1.3, auth) will need a proxy and a dev-loop doc.

- source_spec: `spec-1-1-walking-skeleton.md`
  summary: `eslint-plugin-react-hooks` and `react-refresh` are not configured.
  evidence: The standard Vite `react-ts` template includes both; the scaffold here omits them. Story 1.1's placeholder `App.tsx` uses no hooks, so there is nothing to lint yet. Story 1.2 introduces the hook-using role-aware shell and should add the plugin then.

- source_spec: `spec-1-1-walking-skeleton.md`
  summary: `sprint-status.yaml` carries a truncated story key `1-2-from-scratch-design-system-and-the-role-aware-application-sh`.
  evidence: Pre-existing — generated by the sprint-planning step in commit 13b791a, not introduced by this story. It does not affect story 1.1's key resolution (`1-1-walking-skeleton-with-a-ci-enforced-module-boundary-gate` resolves cleanly). Worth repairing when the 1-2 story is picked up so its build can resolve the key.

- source_spec: `spec-1-3a-identity-backend-and-auth-endpoints.md` (further split at 1.3b planning)
  summary: **1.3b-ii — the frontend Sign up / Log in surface.** `@tanstack/react-query` v5 + `QueryClientProvider` in `app/`; `entities/session` (the `GET /api/auth/me` query, key owned here per AD-16) consuming the generated `shared/api` client; `features/auth` (the auth form: the 1.2 `RoleToggle` with the form defaulting to **Company** and the **Job Seeker option shown but disabled with a short "coming soon" note** until story 1.4; sign-up ↔ log-in mode switch; a required name field labelled "Company name" shown in sign-up mode only, above email; validation on blur per-field and again on submit, never per keystroke; inline errors below the field in the danger token, `aria-describedby`-associated; duplicate-email registration → "This email is already registered as a Company." under the email field with the other entered values retained; failed sign-in → "That email and password don't match. Please try again."; a `GET /api/auth/csrf` fetch seeding the `X-CSRF-TOKEN` header and `credentials: 'include'` on the mutating calls); `pages/sign-in` wired into `app/App.tsx` at `/sign-in` (the nav link's real destination — replaces the story-1.2 not-found seam); on success invalidate/refetch `me`, re-render the shell into the Company signed-in state, navigate to `/`; `widgets/app-shell` — widen `viewer` to `{ kind: 'company'; displayName }` fed from the `me` query, and a signed-in Company `NavBar` (Search + the display name + a Log out affordance calling `POST /api/auth/logout`; no dead links to the Epic-2 "Post a Job" / "My Postings" surfaces); the Vite dev-server `server.proxy` for `/api` → the Host (also covers the story-1.1 deferred "no Vite /api proxy" item).
  evidence: Story 1.3 was split at 1.3a planning into 1.3a (backend, done) and 1.3b; at 1.3b planning the user split 1.3b again into **1.3b-i** (`spec-1-3b-openapi-and-nswag-client-pipeline.md` — OpenAPI 3.0 doc emission + NSwag client + committed `shared/api` + CI drift gate) and **1.3b-ii** (this entry — the frontend auth surface, which consumes 1.3b-i's generated client). All three share the `1-3-company-registration-and-sign-in` sprint-status key. epics.md Story 1.3 ACs for the form, validation, the two prescribed messages, and the shell re-render are 1.3b-ii's scope. The Job-Seeker-disabled-"coming soon" treatment is a user decision recorded here.

- source_spec: `spec-1-3a-identity-backend-and-auth-endpoints.md`
  summary: No rate limiting, request throttling, or account lockout on `POST /api/auth/login` (or `/register`). An automated brute-force / credential-stuffing run against the login endpoint is unthrottled.
  evidence: Raised by code-review pass 1 (blind-hunter) on 1.3a. Not required by the PRD, epics, or the architecture ADs for v1, and out of scope for 1.3a as written; the real fix is ASP.NET Core rate-limiting middleware (partitioned by IP and/or email), its configuration, and tests. Track as an auth-hardening follow-up before or alongside a public deployment.

- source_spec: `spec-1-3b-ii-frontend-sign-in-surface.md`
  summary: `frontend/src/entities/session/model/sessionQuery.ts` `fetchSession` unconditionally returns `{ kind: 'company', … }` from `GET /api/auth/me` without checking `account.accountType`. A non-Company account would be silently rendered as a Company.
  evidence: Raised by code-review pass 1 (blind-hunter, edge-case-hunter) on 1.3b-ii. Not reachable today — the `/api/auth/me` endpoint only ever returns a Company account before story 1.4 — so there is no bad outcome yet. Story 1.4 adds the Job Seeker account space and must widen `Viewer` / `SessionViewer` and branch `fetchSession` on `accountType` (return the Job Seeker viewer, or `null`/error for an unexpected type) at that point.

- source_spec: `spec-1-3b-ii-frontend-sign-in-surface.md`
  summary: A non-`401` failure of `GET /api/auth/me` on load (5xx, network error, 403) leaves a signed-in user on the anonymous shell with no automatic recovery — `useSession` has `retry: false` and `staleTime: Infinity`, so nothing refetches until the key is invalidated or the page is reloaded.
  evidence: Raised by code-review pass 1 (blind-hunter, edge-case-hunter) on 1.3b-ii. The spec deliberately scoped `me` handling to the expected `401` (signed-out) path and chose `retry: false` because a `401` is not transient; a `me` 500 is rare (trivial DB read) and a reload re-runs the query. A robustness follow-up: a bounded retry for non-`401` `me` failures, or an explicit "couldn't load your session" state in `AppShell` instead of silently falling back to the anonymous chrome.

- source_spec: `spec-1-4a-job-seeker-identity-backend.md`
  summary: The `SuccessRehashNeeded` opportunistic password re-hash in `LoginCompanyAsync` and `LoginJobSeekerAsync` (`backend/NexusJob.Modules.Identity/Features/Login/LoginHandler.cs`) catches only `DbUpdateException`. A raw `NpgsqlException` / timeout / connection drop during that best-effort `SaveChangesAsync` turns a verified-correct login into a `500`, contradicting the block's own comment.
  evidence: Raised by code-review pass 1 (edge-case-hunter) on 1.4a. Pre-existing in the Company path since 1.3a; 1.4a mirrors the pattern into the new Job Seeker path (the narrow catch is spec-directed for parity, so it was not patched in 1.4a). Fix: widen both re-hash `catch` blocks consistently to swallow any exception that is not `OperationCanceledException` on the opportunistic re-hash — the login has already succeeded, so a failed re-hash must never surface. Low severity: `SuccessRehashNeeded` only fires when the configured PBKDF2 iteration count changed after the account was created, and `SaveChangesAsync` throwing an unwrapped non-`DbUpdateException` mid-write is rarer still.

- source_spec: `spec-1-4a-job-seeker-identity-backend.md`
  summary: **1.4b — re-enable the Job Seeker option on the frontend Sign up / Log in surface.** In `features/auth/ui/AuthForm.tsx` drop `disabledValues={['jobSeeker']}` and the "Job Seeker accounts are coming soon." note and remove the now-unused `.note` CSS class; in `useAuthForm` translate the `RoleToggle` `Role` to the wire value (`jobSeeker` → `job_seeker`) for both the register and login mutations, make the name-field label ("Company name" / "Full name"), its `autoComplete` (`organization` / `name`), and the duplicate-email message ("…as a Company." / "…as a Job Seeker.") role-aware, and add a `changeRole` that clears field/form errors while keeping entered values; widen `entities/session` `SessionViewer` and `widgets/app-shell` `Viewer` to `company | jobSeeker`, branch `fetchSession` on `account.accountType` (unknown → throw — this closes the `fetchSession` deferred item above), add a `jobSeeker` case to `navItemsFor` (Search + display name + Log out, no Epic-2/3 links) and render the signed-in affordances for any non-anonymous viewer in `NavBar` / `AppShell`; update `AuthForm.test.tsx`, `sessionQuery.test.tsx`, `NavBar.test.tsx`, `App.test.tsx` (the `/sign-in` test currently asserts the disabled Job Seeker option + "coming soon" text).
  evidence: Story 1.4 was split at planning (user decision) into 1.4a (`spec-1-4a-job-seeker-identity-backend.md` — the `job_seeker_account` table, migration, and `accountType` branches in `/api/auth/{register,login,me}` with Testcontainers tests) and 1.4b (this entry — the frontend re-enable, which consumes 1.4a's endpoints). Both share the `1-4-job-seeker-registration-and-sign-in` sprint-status key. epics.md Story 1.4 ACs for the role-toggle relabel, the role-switch value retention, and the Job Seeker signed-in nav are 1.4b's scope. The generated `shared/api` client does not change shape, so there is no build-order dependency beyond 1.4a's endpoints existing.

- source_spec: `spec-2-1a-job-postings-create-endpoint.md`
  summary: **2.1b — the Post-a-Job frontend surface.** A "Post a Job" nav item shown in the shell for a signed-in Company only; `pages/post-a-job` wired at a new route (e.g. `/post-a-job`); a `features/create-posting` slice with the form (a title field and a description field, blur+submit validation, inline errors below the field in the danger token, `aria-describedby`-associated) and a single primary "Publish" action rendered as the `apply-button` component in the accent colour; an `entities/job-posting` slice owning the create mutation / query key and consuming the regenerated `shared/api` client (via an `entities/*/api` segment, AD-16); on success show the confirmation "Your job posting has been published." (Story 2.2 later adds the redirect to the new posting's detail view); on network/server failure retain the entered title and description, show an inline banner "We couldn't publish this posting. Please try again.", and make a retry re-submit the same values; widen `widgets/app-shell` `navItemsFor` so a signed-in Company gets the "Post a Job" item (no dead link for a Job Seeker or anonymous viewer); regenerate `shared/api` from 2.1a's OpenAPI document (no drift). Tests across the new slices plus `NavBar` / `App`.
  evidence: Story 2.1 was split at planning (user decision) into 2.1a (`spec-2-1a-job-postings-create-endpoint.md` — the JobPostings module, `job_postings` schema, `JobPosting` entity, `DbContext`, migration, and `POST /api/job-postings` with Testcontainers tests) and 2.1b (this entry — the Post-a-Job surface, which consumes 2.1a's endpoint and regenerated client). Both share the `2-1-create-a-job-posting` sprint-status key. epics.md Story 2.1 ACs for the "Post a Job" nav item, the form, its validation timing, the publish confirmation, and the save-failure banner are 2.1b's scope. Build-order dependency: 2.1a's endpoint and its OpenAPI operation must exist before the client is regenerated for 2.1b.

- source_spec: `spec-2-1a-job-postings-create-endpoint.md`
  summary: The JobPostings module verbatim-copies cross-cutting infrastructure from Identity — `AntiforgeryEndpointFilter`, `DataAnnotationsValidationFilter<TRequest>`, and the `Build*ConnectionString` schema-forcing helper — with no shared kernel and no parity test, so the two copies can drift silently. The `AntiforgeryEndpointFilter` copies also log nothing on a CSRF rejection (a security-observability gap), and `Build*ConnectionString`'s `catch (Exception e) when (e is ArgumentException or FormatException)` lets any other builder exception abort Host startup.
  evidence: Raised by code-review pass 1 (blind-hunter, edge-case-hunter) on 2.1a. The duplication is spec-directed (AD-1: modules cannot reference each other's implementation, and there is no shared-kernel project), and each copy is behaviorally tested independently, so nothing was patched in 2.1a. Follow-up: introduce a small shared-infrastructure project (or move these into `NexusJob.Host` and pass them through `MapXxxModule`) so Identity and JobPostings share one implementation; while doing so, log antiforgery-validation failures at warning and widen the connection-string `catch` to fall back safely on any exception. Applies equally to the pre-existing Identity copies.

- source_spec: `spec-2-1b-post-a-job-frontend.md`
  summary: Hardening opportunities on the newly-shared `frontend/src/shared/api/http.ts` HTTP plumbing: `ensureCsrfToken` has no in-flight coalescing (two concurrent mutating calls each fire `GET /api/auth/csrf`); `withCredentialsAndCsrf` attaches `X-CSRF-TOKEN` on `POST` only (no `PUT`/`PATCH`/`DELETE`); the CSRF `GET` is issued through `AuthClient` rather than a bare `fetch`, coupling a shared module to one generated domain client; and no surface manages focus on form state transitions (applies to `AuthForm` too).
  evidence: Raised by code-review pass 1 (blind-hunter, edge-case-hunter) on 2.1b. All pre-existing — a verbatim move of the story-1.3b-ii `entities/session/api/csrf.ts` + `authClient.ts` plumbing down into `shared/`; the coalescing gap was raised and rejected on 1.3b-ii (concurrent tokens are both valid for the same session cookie, so the duplicate `GET` is harmless) and the `POST`-only header matches the fact that the v1 API has no non-`POST` mutations. Now that the code is shared by two slices (`entities/session`, `entities/job-posting`) the consolidation is worth doing: coalesce concurrent `ensureCsrfToken` callers behind one in-flight promise, generalise the header to all mutating verbs, and consider a thin `fetch`-based CSRF seed so `http.ts` depends on no generated client. The first non-`POST` mutating endpoint (edit / delete a posting, Epic 3+) forces the verb generalisation at the latest.

- source_spec: `spec-2-2a-detail-endpoint-and-contracts.md`
  summary: **2.2b — the Job posting detail surface.** A `pages/posting-detail` screen wired at a new route (e.g. `/job-postings/:id`); an `entities/job-posting` detail query (a `getById` on the configured `JobPostingsClient`, its query key owned by the slice per AD-16) consuming the regenerated `shared/api` client; a skeleton for the title / description / company block while the query is pending; on `200` render the full title, description, and the posting Company's display name; on `404` show "This posting is no longer available." with a link back to Search; wire Story 2.1b's publish-success path so a Company that just published is redirected to this detail view for the new posting (replacing the in-place `Your job posting has been published.` confirmation); microcopy stays formal. Tests across the new page / query plus the updated `features/create-posting` success path and `App` routing.
  evidence: Story 2.2 was split at planning (user decision, 2026-09-09) into 2.2a (`spec-2-2a-detail-endpoint-and-contracts.md` — `GET /api/job-postings/{id}`, `IIdentityApi` publish + in-process implementation, `IJobPostingsApi` publish, the detail handler resolving the company name via `IIdentityApi`, the ArchUnitNET cross-Contract gate, and the OpenAPI regen, all with Testcontainers tests) and 2.2b (this entry — the detail surface + the publish redirect, which consumes 2.2a's endpoint and regenerated client). Both share the `2-2-view-a-job-posting-s-detail` sprint-status key. epics.md Story 2.2 ACs for the loading skeleton, the `404` "This posting is no longer available." copy with a link back to Search, and the Company redirect-on-publish to the new posting's detail view are 2.2b's scope. Build-order dependency: 2.2a's endpoint and its OpenAPI operation must exist before the client is regenerated for 2.2b.

- source_spec: `spec-2-2a-detail-endpoint-and-contracts.md`
  summary: The Host registers `AddProblemDetails()` but no `UseExceptionHandler`, so an unhandled exception from any endpoint handler (a transient Npgsql error mid-query, an `IIdentityApi` failure) returns a bare HTTP 500 with no RFC 9457 `application/problem+json` body — despite AD-15 requiring ProblemDetails on every non-2xx.
  evidence: Raised by code-review pass 1 (edge-case-hunter) on 2.2a. Pre-existing and app-wide — `CreateJobPostingHandler` (2.1a) and every Identity handler have the same exposure; 2.1a's Design Notes explicitly accept "a `SaveChanges` failure is a genuine 500". Deliberate error responses already use `Results.Problem` (so they are RFC 9457); only the unhandled-exception path is bare. Fix: add `app.UseExceptionHandler()` (it composes with `AddProblemDetails()` to emit a problem+json 500) early in the Host pipeline, plus a test that a forced handler exception yields `application/problem+json`. Touches `NexusJob.Host/Program.cs`, which 2.2a's "Never" list excluded, so it belongs in its own change.

- source_spec: `spec-2-2b-job-posting-detail-surface.md`
  summary: The frontend has no route-change announcement for assistive tech. After a client-side redirect (`SignInPage` and `PostAJobPage` on their `<Navigate>` guards, and now `features/create-posting`'s publish → `/job-postings/:id` redirect) and after `PostingDetailPage`'s async query resolves, there is no `aria-live` region announcing the new page and no `document.title` update, so a screen-reader user gets no cue that the location or page content changed.
  evidence: Raised by code-review pass 1 (blind-hunter, edge-case-hunter) on 2.2b. Pre-existing pattern — the story-1.3b-ii `SignInPage` and story-2.1b `PostAJobPage` redirects already navigate with no announcement; 2.2b adds one more instance. 2.2b's review `patch` restores keyboard-focus continuity on `PostingDetailPage` (focus moves to the `<h1>` / error `<p>` on settle), but that is per-page. The proper fix is an app-level route-change announcer (a visually-hidden `aria-live="polite"` region in `AppShell` updated on `useLocation()` change, plus per-route `document.title`), which touches the shell and every page — its own a11y-hardening change, not 2.2b's scope. No WCAG 2.1 AA success criterion is definitively breached (4.1.3 is satisfied for the loading state; 2.4.3 focus order is acceptable), so this is a polish / SR-experience gap.

- source_spec: `spec-2-3a-search-job-postings-endpoint.md`
  summary: **2.3b — the Home / Search landing surface.** Wire `HomePage`'s search input/button to an explicit-submit form (no as-you-type) driving `entities/job-posting`'s new `useJobPostingSearch(query, page, pageSize)` query hook against 2.3a's `GET /api/job-postings` endpoint; a new `JobPostingCard` + `JobPostingCardSkeleton` in `entities/job-posting/ui` (whole-card link to the detail route, DESIGN.md hover elevation as the only hover affordance, no Apply control); a new generic `Pagination` control (Prev/Next + "Page X of Y", disabled at bounds) in `shared/ui`; `HomePage` render branches for pending (skeleton rows), error ("We couldn't run that search. Please try again." + a retry that re-submits the same keyword), empty-catalog ("No open postings yet. Check back soon." when the submitted keyword is empty), no-match (`No postings match "{keyword}." Try a different term.`), and success (the card stack + pagination). No new route — Home (`/`) is both landing and results; no URL-driven search state. Tests across the new query hook, card, skeleton, pagination control, and `HomePage`.
  evidence: Story 2.3 was split at planning (user decision, 2026-09-09, token gate — the combined spec was ~4,500 tokens) into 2.3a (`spec-2-3a-search-job-postings-endpoint.md` — the anonymous `GET /api/job-postings?query=&page=&pageSize=` slice, the module-local `Page<T>` shape, the batched `IIdentityApi.GetCompanies` company-name resolution, `ILIKE` substring matching with wildcard-escaping, and the OpenAPI regen, all with Testcontainers tests) and 2.3b (this entry — the Home search/browse surface, which consumes 2.3a's endpoint and regenerated client). Both share the `2-3-search-job-postings-by-keyword` sprint-status key. epics.md Story 2.3 ACs for the search-bar submit behavior, the `job-card` list/pagination, the loading skeleton, and the empty/no-match/error copy are 2.3b's scope. Build-order dependency: 2.3a's endpoint and its OpenAPI operation must exist before the client is regenerated for 2.3b.

- source_spec: `spec-2-3a-search-job-postings-endpoint.md`
  summary: The built-in ASP.NET Core OpenAPI document generator omits `"type": "integer"` from the schema for plain `int` properties (only `pattern`/`format: int32` are emitted) — confirmed on this story's new `page`/`pageSize`/`total` (`PageOfJobPostingSearchResultResponse`) and on the pre-existing framework `ProblemDetails.status`, so it is systemic, not introduced by this story. Because NSwag relies on `schema.type` to pick a TypeScript type, the regenerated client emits `any` for `search()`'s `page`/`pageSize` parameters and for `page`/`pageSize`/`total` on the response, losing compile-time protection for the next story (2.3b) that will consume them as pagination cursors.
  evidence: Code review (blind-hunter + edge-case-hunter) on 2.3a. Verified by reading `backend/NexusJob.Host/openapi/NexusJob.Host.json`: `components.schemas.PageOfJobPostingSearchResultResponse.properties.{page,pageSize,total}` and `components.schemas.ProblemDetails.properties.status` all carry `pattern`/`format` but no `type`. Fixing this is a cross-cutting OpenAPI-pipeline change (affects every `int` property emitted by the app), out of 2.3a's scope; 2.3b's implementer should re-check whether it's been fixed before hand-typing around the `any`s.

- source_spec: `spec-2-3a-search-job-postings-endpoint.md`
  summary: The `dev`-profile Docker dev-tooling added earlier in this session (`docker-compose.yml`'s `backend`/`frontend` services, `backend/Dockerfile`, `frontend/Dockerfile`, both `.dockerignore` files, `vite.config.ts`'s `API_PROXY_TARGET` proxy target) has zero CI coverage — `ci.yml`'s only Docker job (`image`) runs plain `docker compose build`/`up -d` with no `--profile dev`, so these Dockerfiles/services are never built or started in CI — and has a few minor robustness gaps: the `backend` dev service has no `healthcheck` and `frontend`'s `depends_on: backend` has no `condition`, so Vite's proxy can start forwarding before `dotnet watch`'s build finishes; the local Postgres password is now hardcoded a third time across `docker-compose.yml`'s three services instead of one shared value; and `vite.config.ts`'s `process.env.API_PROXY_TARGET ?? 'http://localhost:2052'` would silently break every `/api` request if the variable were ever set to an empty string rather than left unset (no shipped config does this today).
  evidence: Code review (blind-hunter + verification-gap) on 2.3a; these files predate 2.3a's implementation (added earlier in the same session, unrelated to the search endpoint) and were swept into the reviewed diff only because `baseline_commit` was captured after they were already uncommitted. Verified via `grep -r profile .github` (no `--profile dev` anywhere) and by reading `docker-compose.yml`'s `backend`/`frontend` service definitions.

- source_spec: `spec-2-3a-search-job-postings-endpoint.md`
  summary: The existing 2.3b deferred-work.md entry's empty-catalog copy condition ("No open postings yet… when the submitted keyword is empty") doesn't also state the accompanying "and total is 0" qualifier, leaving it ambiguous whether an empty keyword against a non-empty catalog should render the empty-catalog message or the normal results list — which would conflict with 2.3a's own "missing/empty query = browse everything" semantics if read literally.
  evidence: Code review (blind-hunter) on 2.3a. The entry predates 2.3a's implementation (written when story 2.3 was split); 2.3b's implementer should resolve the wording before building the Home surface's empty-state branch.

- source_spec: `spec-3-1a-applications-module-and-apply-endpoints.md`
  summary: **3.1b — the Apply surface on the posting detail page.** The three-state apply-button on `pages/posting-detail` (signed-out Job Seeker → opens the apply-gate modal, which is Story 3.2's scope; signed-in Job Seeker who has not applied → inline submit with no navigation, then relabel to a disabled "Applied" state with "Your application has been submitted."; signed-in Job Seeker who already applied → renders disabled "Applied" on load); the on-load state driven by a new `entities/application` query against 3.1a's `GET /api/applications/mine?jobPostingId=`; the inline submit calling `POST /api/applications` with `{ jobPostingId }`; the inline-failure branch ("We couldn't submit your application. Please try again." with a retry that keeps the Job Seeker's place); the apply-button as the single accent-colored primary action on the surface; and the AR-10 bounded-context-mirror checkpoint note assessing whether the "frontend slice mirrors backend bounded context" mapping paid off, written before the pattern spreads to any other slice. Consumes 3.1a's endpoints and the regenerated `shared/api` client. FSD downward-imports-only; the generated client imported only from an `entities/*/api` segment; TanStack Query keys owned by `entities/application`.
  evidence: Story 3.1 was split at planning (user decision, 2026-09-10) into 3.1a (`spec-3-1a-applications-module-and-apply-endpoints.md` — the Applications module with its own `applications` schema / `DbContext` / migration history, the `application` table and its `UNIQUE (job_posting_id, job_seeker_id)` constraint, `POST /api/applications` with idempotent 23505 handling (200 with the existing row, never 409/500, no pre-check SELECT), `GET /api/applications/mine?jobPostingId=`, the `IJobPostingsApi.GetPostingOwner` existence check, and the OpenAPI regen, all with Testcontainers tests) and 3.1b (this entry — the apply surface + mirror-slice checkpoint, which consumes 3.1a's endpoints and regenerated client). Both share the `3-1-apply-to-a-posting` sprint-status key. epics.md Story 3.1 ACs for the apply-button states, the inline no-navigation submit, the confirmation copy, and the on-load applied state are 3.1b's scope. Build-order dependency: 3.1a's endpoints and their OpenAPI operations must exist before the client is regenerated for 3.1b. The signed-out apply-gate modal itself is Story 3.2, not 3.1b.

- source_spec: `spec-3-1a-applications-module-and-apply-endpoints.md`
  summary: `_bmad-output/implementation-artifacts/epic-3-context.md` still lists a single un-split "Story 3.1" (attributing the apply surface + AR-10 checkpoint to it) and phrases the `GET /api/applications/mine` payload as two shapes (`{ applied, appliedAt }` or `{ applied: false }`), which the shipped 3.1a endpoint contradicts — `appliedAt` is always present (null when not applied).
  evidence: Blind-hunter review on 3.1a (findings #11/#12). The context file is a faithful distillation of `epics.md`, which still carries one un-split Story 3.1; the 3.1a/3.1b split is a build-time decision tracked in the 3.1a spec, this file, and `sprint-status.yaml`. No implementer was misled (the 3.1a spec is precise and the implementation is correct). Refresh epic-3-context.md — split the Stories list into 3.1a (backend: module, schema, `POST /api/applications`, `GET /api/applications/mine`) and 3.1b (apply surface + AR-10 mirror-slice checkpoint), and correct the `/mine` description to the single always-`appliedAt` shape — the next time Epic 3 planning is touched or `compile-epic-context` is re-run.

- source_spec: `spec-3-3a-my-applications-endpoint.md`
  summary: **3.3b — the My Applications page.** A role-aware "My Applications" nav item for a signed-in Job Seeker (`widgets/app-shell/NavBar.tsx`'s `navItemsFor` already has a placeholder comment for it), a new `/my-applications` route guarded the same way as `PostAJobPage` (redirect non-Job-Seekers to `/`, render nothing while the session is unresolved), a paged list of applied postings (skeleton rows while loading, each row showing the posting title, `Pagination` reuse from `shared/ui`), and the empty state "You haven't applied to anything yet." with a link to Search. Consumes 3.3a's new `GET /api/applications/mine/list` endpoint and the regenerated `shared/api` client (`applicationsClient.getMyApplications`, a new `entities/application` query hook).
  evidence: Story 3.3 was split at planning (user decision, 2026-09-12) into 3.3a (`spec-3-3a-my-applications-endpoint.md` — the new `GET /api/applications/mine/list?page=&pageSize=` endpoint on its own operation id, a new `(job_seeker_id, submitted_at)` index, the batched `IJobPostingsApi.GetPostingSummaries` title resolution, and the OpenAPI regen, all with Testcontainers tests) and 3.3b (this entry — the My Applications surface, which consumes 3.3a's endpoint and regenerated client). Both share the `3-3-a-job-seeker-sees-the-postings-they-applied-to` sprint-status key. epics.md Story 3.3's nav-item, skeleton, and empty-state ACs are 3.3b's scope. Build-order dependency: 3.3a's endpoint and its OpenAPI operation must exist before the client is regenerated for 3.3b. Also user-decided (2026-09-12): the paged list is a NEW dedicated route/operation id, not an overload of the existing `GET /api/applications/mine` — 3.1a's Design Notes had speculatively anticipated overloading the same route, but investigation found no precedent anywhere in this codebase for one route serving two 200 shapes and a real risk of NSwag silently generating a wrong/loose client type for such a response; a distinct route sidesteps that risk entirely.

- source_spec: `spec-3-3a-my-applications-endpoint.md`
  summary: The regenerated `frontend/src/shared/api/nexus-api-client.ts` types `ApplicationsClient.getMyApplications`'s `page`/`pageSize` parameters and `PageOfMyApplicationListItemResponse.{page,pageSize,total}` as `any` instead of `number`.
  evidence: Code review (blind-hunter) on 3.3a. Same systemic root cause already tracked for 2.3a's `JobPostingsClient.search` (deferred-work.md entry: the built-in ASP.NET Core OpenAPI generator omits `"type": "integer"` for plain `int` properties, so NSwag falls back to `any`) — confirmed here as the identical pattern on a fresh endpoint, not a new bug. 3.3b's implementer should either re-check whether the cross-cutting OpenAPI-pipeline gap has been fixed, or re-type the field in `entities/application/api`'s wrapper the same way `jobPostingsClient.search` already does.

- source_spec: `spec-3-3a-my-applications-endpoint.md`
  summary: `epic-3-context.md` still lists a single un-split "Story 3.3" and does not mention the 3.3a/3.3b split (the new `GET /api/applications/mine/list` endpoint vs. the still-pending My Applications surface).
  evidence: Code review (blind-hunter) on 3.3a. Same documentation-drift class already logged for the 3.1a/3.1b split (see the earlier deferred-work.md entry on this file). The 3.3a spec itself is precise and no implementer was misled; refresh epic-3-context.md's Stories list and Cross-Story Dependencies for 3.3a/3.3b (alongside the still-outstanding 3.1a/3.1b refresh) the next time Epic 3 planning is touched or `compile-epic-context` is re-run.
