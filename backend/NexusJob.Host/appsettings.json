{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.AspNetCore.HttpLogging.HttpLoggingMiddleware": "Information"
    }
  },
  "AllowedHosts": "*",
  "Auth": {
    "PasswordHasher": {
      "//": "PBKDF2 iteration count for password hashing (AD-13). Left unset here so the code default of 600000 (current OWASP figure) applies. Real deployments override it via the Auth__PasswordHasher__IterationCount environment variable; it must never drop below 600000."
    }
  }
}
